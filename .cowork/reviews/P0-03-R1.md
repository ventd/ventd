# Review P0-03-R1
Task: P0-03
PR: https://github.com/ventd/ventd/pull/238
Tip SHA: 483977691a7eb54de22761b73b6a6109e046da6a
Verdict: revise (R6 — stale base, CHANGELOG conflict)

Checklist:
  R1:  ✅ Draft, Task ID in body.
  R2:  ✅ Branch claude/FOUND-hwissue-7b2f8 matches assigned.
  R3:  ✅ Two commits, both conventional:
         - 009c3fd docs: add hardware-report issue template (P0-03)
         - 4839776 docs: add hardware-report changelog entry
       Minor: second commit lacks the (P0-03) suffix. Not a hard
       violation; not worth a revision round.
  R4:  ✅ Files touched == allowlist: .github/ISSUE_TEMPLATE/hardware-report.yml (new) + CHANGELOG.md.
  R5:  ✅ No test files touched.
  R6:  ❌ mergeable_state=dirty. CI has not run (0 check runs). Root cause:
       CC's branch base is fb581b9 (pre-P0-02 main). Between P0-02 merging
       at 4aa6a37 and P0-03 being pushed, main advanced one commit, but
       CC's checkout didn't pull. Both P0-02 and P0-03 added an `### Added`
       subsection immediately beneath `## [Unreleased]`, so the three-way
       merge produces a conflict in CHANGELOG.md. No CC content error
       caused this — standard rebase fixes it.
  R7:  ✅ Template content fully correct.
       - GitHub Forms schema matches bug.yml / regression.yml.
       - Title prefix `[hw] `, labels `["hardware-report"]`.
       - Markdown preamble explicitly states no device serials, product UUIDs,
         or SMBIOS identifiers are requested. Correct privacy posture.
       - DMI strings field uses `render: shell` with a one-liner that reads
         exactly the eight permitted files under /sys/class/dmi/id/ — no
         product_serial, no board_serial, no product_uuid. Matches the
         prompt's PII-exclusion requirement.
       - Sensor chip, fan count, distro, probe output, system notes all
         present with the correct shape (`input` vs `textarea`, `render: shell`
         where appropriate).
       - First five groups have `validations: required: true`; the sixth
         (system notes) is optional by omitting the validations block.
       Per testplan §17 P0-03 DoD: "template visible when filing new
       issue" — template is valid GitHub Forms YAML with no schema errors
       (CC's python yaml.safe_load passed; the GitHub Forms validator will
       accept this on upload).
  R8:  ✅ No new dependencies.
  R9:  ✅ No safety-critical paths touched.
  R10: ✅ No secrets / real hardware fingerprints. Privacy posture is
       explicitly engineered into the template.
  R11: ✅ CHANGELOG entry added under ## Unreleased / ### Added (subject to
       the conflict resolution).
  R12: ✅ No public API change.
  R13: ✅ Single track (FOUND).
  R14: n/a CI hasn't run; no code in diff.
  R15: ✅ No binary produced.
  R16: ✅ No code added.
  R17: ✅ No goroutine added.
  R18: ✅ No I/O added.
  R19: ✅ No `Fixes:` line → no regression test required.
  R20: ✅ No safety-critical function added.
  R21: ✅ No goroutine.
  R22: ✅ No backend implementation.
  R23: ✅ No public metric / route / config field.

Notes:
  The fix is mechanical. CC rebases claude/FOUND-hwissue-7b2f8 onto
  origin/main (4aa6a37 or later), resolves the CHANGELOG.md conflict
  by keeping both `- docs: add public roadmap (#P0-02)` and
  `- docs: add hardware-report issue template (#P0-03)` under the same
  `### Added` subsection, force-pushes, and reports a new AFTER-SHA.
  Template content does not change.

  Revision round 1 of 3 for P0-03.
