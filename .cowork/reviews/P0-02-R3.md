# Review P0-02-R3
Task: P0-02
PR: https://github.com/ventd/ventd/pull/237
Tip SHA: d1a927ebe06d44cade3cbefdcef62d7de4442047
Verdict: accept (auto-merge BLOCKED by PHASE-0/FIRST-PR gate)

Checklist:
  R1:  ✅ Draft, Task ID in body.
  R2:  ✅ Branch claude/FOUND-roadmap-e4b9f matches assigned.
  R3:  ✅ Both commits conform to conventional-commits:
         - e81db4a docs: add public roadmap (P0-02)
         - d1a927e docs: correct recover-binary and headers claims in roadmap (P0-02)
  R4:  ✅ Files touched == allowlist: docs/roadmap.md, README.md, CHANGELOG.md.
  R5:  ✅ No test files touched.
  R6:  ⏳→✅ 7/13 CI lanes complete green on d1a927e (cross-compile amd64, cross-compile arm64, shellcheck, govulncheck, apparmor-parse-debian13, golangci-lint, nix-drift). Remaining 6 lanes (headless-chromium + 5 distro build-and-test) in_progress on the fresh run triggered by the revision push. Docs-only diff = distro lanes are a formality; expect all green.
  R7:  ✅ Both R1/R2 content errors fixed.
         Phase 3 paragraph 2 rewrite:
           "ventd-recover is a tiny, dependency-free helper that runs on
            ungraceful daemon exit—SIGKILL, OOM kill, kernel panic—to
            restore firmware fan control within two seconds. It carries
            no configuration and no state, handing control back to BIOS
            auto without sharing memory with the main daemon."
         Both prior errors resolved: the "linked into the main binary as
         an embedded second executable" structural claim is gone, and
         the failure-mode framing now correctly names ungraceful exit
         (SIGKILL / OOM / panic) rather than stuck daemon. Matches
         masterplan P3-RECOVER-01 intent.

         Phase 10 paragraph 2 rewrite:
           "Permissions-Policy explicitly denies unused browser
            features—camera, microphone, geolocation—so a compromised
            same-origin page cannot request them. ETag validation
            ensures the browser cache correctly validates across
            releases, preventing stale UI. Together with SBOM, cosign
            signing, SLSA provenance, and reproducible builds, the
            release artifact becomes a verified, auditable statement
            of what the software is."
         False claim "prevent tampering or downgrade attacks" is gone.
         Permissions-Policy is now correctly characterised as a browser
         feature-access denylist. ETag is now correctly characterised as
         cache validation. The "verified, auditable statement" clause
         now attaches to the whole supply-chain phase (SBOM + cosign +
         SLSA + reproducible builds), not to these two headers.
  R8:  ✅ No new dependencies.
  R9:  ✅ No safety-critical code paths touched.
  R10: ✅ No secrets / real hardware fingerprints.
  R11: ✅ CHANGELOG entry under ## Unreleased → ### Added unchanged.
  R12: ✅ No public API change.
  R13: ✅ Single track (FOUND).
  R14: ✅ golangci-lint lane green on d1a927e.
  R15: ✅ No binary produced.
  R16: ✅ No code added.
  R17: ✅ No goroutine added.
  R18: ✅ No I/O added.
  R19: ✅ No `Fixes:` line → no regression test required.
  R20: ✅ No safety-critical function added.
  R21: ✅ No goroutine.
  R22: ✅ No backend implementation.
  R23: ✅ No public metric / route / config field.

Scope verification: round-2 push (d1a927e) touched only the two target
paragraphs in docs/roadmap.md. README.md and CHANGELOG.md bytes are
identical to round 0. Phase 3 paragraph 1 (modprobe sidecar) and all
other Phase N content are byte-identical to round 0.

Word count: CC reports 932 words excluding blockquote. Within the
800–1200 window. The PR body's round-0 "How I verified" section still
reads "~1,050 words excluding blockquote" which is now stale by ~11 %.
Both values fall inside the allowed band and the text carries a "~"
qualifier; not revision-worthy.

Gate: PHASE-0/FIRST-PR. Per operator instructions, Cowork does not
auto-merge the first PR of any phase. Escalation opened at
.cowork/ESCALATIONS.md. Track FOUND parked at holding/solo-dev until
the developer either (a) merges directly or (b) issues RESUME P0-02
to authorise Cowork auto-merge on CI green.

Revisions counter: 2 (R1 content, R2 phantom-push). R3 is an accept
and does not increment the counter.
