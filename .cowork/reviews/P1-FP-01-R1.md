# Review P1-FP-01-R1
Task: P1-FP-01
PR: https://github.com/ventd/ventd/pull/246
Head SHA: 91e18b5dc5fa586d853024b0e352c03af71f49c9
Verdict: escalate (PHASE-1/FIRST-PR + plan-interpretation)
Reviewer: Cowork (Opus 4.7)
Timestamp: 2026-04-18T (session resume)

Checklist:
  R1 (draft + task ID in body): OK
  R2 (branch): OK — claude/fingerprint-database-yaml-jFBaq
  R3 (conventional commits): OK
  R4 (allowlist subset): OK — prompt allowlist `internal/hwdb/**, internal/hwmon/autoload.go, CHANGELOG.md`; diff exactly those four files.
  R5 (no test files): OK
  R6 (CI): OK — 13/13 green
  R7 (DoD): PARTIAL — prompt said "old map deleted"; CC did not delete `knownDriverNeeds` because `internal/hwmon/dmi.go` and `internal/hwmon/install.go` also consume that map, both outside the allowlist. CC's reasoning (map has two functions — DMI-trigger lookup now obsolete, and out-of-tree driver install metadata still used by install.go) holds. This is a plan-interpretation ambiguity: follow the allowlist and skip the delete, or widen the allowlist to honour the DoD.
  R8 (no new deps): OK — yaml.v3 already direct dep
  R9 (safety preserved): OK — hwdb is advisory, probe-time only, never touches the controller hot path
  R10 (secrets): OK
  R11 (CHANGELOG): OK
  R12 (public API): New `internal/hwdb` package; internal scope only. OK.
  R13-R18: OK
  R19-R23: N/A (no Fixes, no safety-critical change, no new goroutine, T-FP-01 will cover tests)

Additional notes from CC:
  - 22 total profile entries, 20 flagged `unverified: true`. Only the two MSI MAG/MPG triggers from the original `knownDriverNeeds` are not flagged. Gigabyte entry is flagged because it was previously only a driver proposal, not an autoload. Verification flag is the documented mechanism for callers to treat entries as tentative. Reasonable.
  - `Load()` re-parses YAML on every `Match` call. Fine for probe-time (one call) per CC's note. Future hot-loop callers will need a cached variant.

Why escalate rather than accept or revise:
  - PHASE-1/FIRST-PR rule.
  - Plan interpretation: task catalogue says "old map deleted" as a DoD item. CC's partial-completion is defensible under the allowlist constraint, but masterplan §14 forbids Cowork from reinterpreting the plan. Developer decides: (a) RESUME P1-FP-01 as partial (accepts the out-of-scope delete as a follow-up task), (b) REWRITE P1-FP-01 with widened allowlist (`internal/hwmon/**`) so the delete lands in the same PR, or (c) DROP and re-scope.

Recommended action: RESUME as partial. The prompt's allowlist constraint is the stronger signal; widening allowlists to chase DoD items retroactively sets a bad precedent. Track the knownDriverNeeds retirement as a new task whose allowlist includes dmi.go/install.go explicitly.
