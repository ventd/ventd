You are Claude Code, working on the ventd repository.

## Task
ID: P1-FP-02
Track: FP
Goal: Opt-in remote refresh for the hwdb fingerprint database. Fetches `profiles.yaml` from `ventd/hardware-profiles` over HTTPS, verifies SHA-256 against a locally-pinned digest, merges into the in-memory DB. Gated OFF by default.

## Model
Sonnet 4.6 (well-bounded networking + merge logic; no new safety surface).

## Context you should read first

- `internal/hwdb/profiles.yaml` — the embedded DB from P1-FP-01.
- `internal/hwdb/` — the package structure after P1-FP-01.
- `cmd/ventd/listfansprobe.go` — where probe is invoked.
- `internal/config/config.go` — for the `hwdb.allow_remote` flag you'll add.

## What to do

1. Add a new file `internal/hwdb/refresh.go` implementing:
   - A single exported `RefreshFromRemote(ctx, client, pinnedSHA256) (count int, err error)` function.
   - Issues an HTTPS GET against a hard-coded upstream URL (`https://raw.githubusercontent.com/ventd/hardware-profiles/main/profiles.yaml` — make this a const).
   - Reads response body, computes SHA-256, compares against `pinnedSHA256` (caller-supplied). On mismatch: return error without mutating DB.
   - On match: parse YAML, merge into the in-memory DB via a merge helper (local entries win on conflict).
   - Emits one structured `slog.Info` event per refresh: `{kind: "hwdb-refresh", count: N, sha: "..."}`.

2. Add a config field `Config.HWDB.AllowRemote bool` (yaml `hwdb.allow_remote`, default `false`). Wire validation.

3. Extend `cmd/ventd/listfansprobe.go` (or wherever `--probe` runs) so `ventd --probe-modules --refresh-hwdb` flag triggers `RefreshFromRemote` when `AllowRemote` is true.

4. No startup network calls. The daemon boot path must not hit the network. Remote refresh is CLI-triggered only.

5. Add a small unit test in `internal/hwdb/refresh_test.go` using `httptest.NewServer` to verify:
   - SHA mismatch returns error, DB unchanged.
   - SHA match parses + merges.
   - Context cancellation propagates.
   - Flag-off path skips the call entirely.

6. Run `go test -race -count=1 ./internal/hwdb/...` and `go vet ./...` — clean.

## Definition of done

- `internal/hwdb/refresh.go` exists with `RefreshFromRemote`.
- `Config.HWDB.AllowRemote` field with validator, default `false`.
- CLI flag wired in `cmd/ventd/listfansprobe.go`.
- Unit tests pass under `-race`.
- Zero network calls at daemon startup (verify by reading the init path).
- `go vet` + `golangci-lint` clean.
- `CGO_ENABLED=0 go build ./cmd/ventd/` succeeds.
- CHANGELOG.md `## Unreleased` / `### Added` entry: one line referencing P1-FP-02.

## Out of scope for this task

- Tests outside the scope this task targets per the testplan catalogue. Only the refresh_test.go unit test described above.
- A scheduled/periodic refresh loop. This task is CLI-trigger only.
- Changes to `ventd/hardware-profiles` repo. That repo already exists per Developer actions §13.2.
- Signing/verification beyond SHA-256 pin. (Future: cosign signature — not this task.)
- Adding new deps beyond stdlib `crypto/sha256`, `net/http`, `gopkg.in/yaml.v3` (already present).

## Branch and PR

- Work on branch: `claude/P1-FP-02-hwdb-remote-refresh`
- Commit style: conventional commits.
- Open a draft PR on completion with title: `feat(hwdb): opt-in remote refresh from ventd/hardware-profiles (P1-FP-02)`
- PR description: goal verbatim, files-touched list, "How I verified" with test output, task ID: P1-FP-02.

## Constraints

- Do not touch files outside: `internal/hwdb/**`, `cmd/ventd/listfansprobe.go`, `internal/config/config.go` (just the new field), `CHANGELOG.md`.
- No new dependencies outside stdlib + already-present `gopkg.in/yaml.v3`.
- Preserve all safety guarantees.
- No startup network calls.

## Reporting

- STATUS: done | partial | blocked
- PR: <url>
- SUMMARY: <= 200 words
- CONCERNS: any second-guessing
- FOLLOWUPS: work you noticed out of scope
