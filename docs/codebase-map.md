# ventd codebase map

A surface-by-surface navigation map of the whole repository: what each directory
is for, the key types and entry points to grep for, and the engineering-invariant
rule file that governs each subsystem. Read this first when you need to find
where something lives; open the cited files only when you need the full text.

- Module: `github.com/ventd/ventd` · Go **1.25.10** · `CGO_ENABLED=0` single static binary
- ~174k LOC Go · ~90 internal packages · 388 `*_test.go` files · 62 rule files
- Companion index of invariants: [`docs/rules/INDEX.md`](rules/INDEX.md) (regenerated by `go run ./tools/rule-index`)
- Narrative for newcomers: [`README.md`](../README.md) · API contract: [`docs/api.md`](api.md)

> **Keeping this current.** This file is a hand-maintained map, not generated.
> When you add a package, an `/api` route, a HAL backend, a CI workflow, or a
> rule file, update the matching section here. The numeric counts above and the
> catalog counts below are the most likely things to drift.

---

## 1. Repo at a glance

| Path | What it is |
|------|------------|
| `cmd/` | Binary entrypoints: `ventd` (daemon + all subcommands), `ventd-recover`, `ventd-ipmi`, `ventd-nvml-helper`, `ventd-soak` |
| `internal/` | All application code (~90 packages). Grouped in §4–§6 below |
| `web/` | Static frontend: vanilla HTML/CSS/JS pages, no framework, no build step, embedded via `web/embed.go` |
| `docs/` | Guides + `docs/rules/` (engineering invariants) + `docs/rules-rationale/` + screenshots. See §8, §11 |
| `deploy/` | systemd units, AppArmor/SELinux profiles, udev rules, sysusers/tmpfiles. See §10 |
| `packaging/` | docker / aur / nix packaging |
| `scripts/` | Install lifecycle, CI-local mirror, init-system files, release. See §9 |
| `tools/` | Repo-tooling enforcers (rulelint, rule-index, allowlistcheck, …). See §9 |
| `validation/` | Tiered HIL/VM test records and smoke harnesses. See §10 |
| `testutil/`, `internal/testfixture/` | Test helpers and hardware fakes. See §10 |
| `Makefile`, `.goreleaser.yml`, `.golangci.yml`, `go.mod` | Build/lint/release config. See §9 |
| `.github/workflows/` | 20 CI workflows; `ci.yml` is the canonical gate. See §9 |
| `config.example.yaml` | Annotated reference config. See §11 |
| `CHANGELOG.md` | ~298 KB; per-release history (`[Unreleased]` accrues bullets) |

**What ventd is**, in one line: a Linux fan-control daemon with a browser UI,
auto-detection across 11+ hardware backends, an experimental learned ("smart")
controller, and an honest monitor-only fallback when hardware can't be controlled.

---

## 2. Binaries (`cmd/`)

| Binary | Role | Entry / notes |
|--------|------|---------------|
| `cmd/ventd/` | Main daemon **and** all operator subcommands | `main.go` `run()` (~2280 lines). Subcommand dispatch happens **before** `flag.Parse()` because subcommands own conflicting flag sets |
| `cmd/ventd-recover/` | Emergency one-shot: walk `/sys/class/hwmon/hwmon*/pwm<N>_enable`, write `1` (firmware-auto) | Fired by `ventd-recover.service` via `OnFailure=`. **Always exits 0** so the OnFailure chain can't loop. Alloc-light |
| `cmd/ventd-ipmi/` | Privilege-separated IPMI sidecar; sole holder of `/dev/ipmi0` (`CAP_SYS_RAWIO`) | Serves the daemon over a Unix socket using `internal/hal/ipmi/proto` (length-prefixed JSON) |
| `cmd/ventd-nvml-helper/` | SUID-root (`04755`) helper proxying whitelisted NVML *write* calls so the daemon can stay unprivileged | `/usr/local/sbin/ventd-nvml-helper`; stable exit codes 0–5; recursion guard |
| `cmd/ventd-soak/` | Read-only soak/field-validation harness; decodes persisted Layer-B coupling shards and prints convergence | Safe alongside production (`snapshot`/`watch` never write). `excite` needs `--enable-soak-excitation`. Not shipped by goreleaser |

**`ventd` subcommands** (all in `cmd/ventd/`): `diag bundle` / `diag export-observations` (`diag*.go`),
`doctor` (`doctor.go`), `preflight` (`preflight.go`, `preflightcheck.go`), `calibrate --acoustic`
(`calibrate_acoustic.go`), `import-sensors-conf` (`import_sensors_conf.go`), `power-profile`
(`power_profile.go`), `version`. Daemon-mode flags include `--setup`, `--probe-modules`,
`--rescan-hwmon`, `--recover`, `--list-fans-probe`, `--calibrate-probe`, `--preflight-check`,
the four experimental `--enable-*` flags, and `--strict-idle-gate`.

**Privileged vs sandboxed split:** `--probe-modules` / `--rescan-hwmon` / `--recover` are
privileged one-shots run by the installer **outside** the systemd sandbox. The long-running
daemon **never** modprobes — under `ProtectKernelModules=yes` those syscalls would EPERM.

---

## 3. The control tick and smart-mode data flow

This is the spine of the daemon. Read `cmd/ventd/main.go` (`run`, `runDaemonInternal`,
the `SmartModeBundle`) and `cmd/ventd/smart_builders.go` (`buildBlendFn`) alongside this.

**Bootstrap (`run()`):** subcommand dispatch → `config.LoadForStartup` (in-band retry absorbs
cold-boot udev ENOENT races, issue #103) → `calibrate.VerifyAllowStopGate` (refuse to start if a
fan has `allow_stop=true` without stall calibration, #600) → first-boot TLS self-sign + auth load
→ `probe.Probe` builds `[]*probe.ControllableChannel` → `registerHALBackends` + `hal.Enumerate`
→ `sysclass.Detect` → construct the `SmartModeBundle`. On first boot an inline background
**polarity auto-probe** runs under watchdog coverage; controllers refuse writes until polarity
persists (~115 s for 8 channels).

**SmartModeBundle (`main.go`)** bundles the learning runtimes derived in `run()`: signature
`Library`, Layer-B `coupling.Runtime`, Layer-C `marginal.Runtime`, Layer-A `layer_a.Estimator`,
`aggregator.Aggregator`, the `controller.BlendedController`, a `DecisionCache`, and the channel
slice. **Every builder returns nil in monitor-only mode** (no controllable channels), making all
downstream wiring a no-op.

**The per-fan reactive tick (`internal/controller/controller.go` `tick()`):**
1. Read `liveCfg` once (atomic pointer, swapped on SIGHUP).
2. Yield the tick if the channel is calibrating or panicked.
3. Read all sensors (flaky sensors isolated; sentinel values carried forward), EMA-smooth.
4. Evaluate the compiled curve (the PI curve is the only `StatefulCurve` — integral state carries across ticks).
5. **Clamp to `[MinPWM, MaxPWM]`** — the hard safety layer; the curve can never exceed operator bounds.
6. Run the blend hook (below) if wired.
7. Re-clamp, apply ramp-**down**-only hysteresis, then `polarity.WritePWM` (inversion-correct), then `ObsAppend`.
   PWM=0 is refused unless `MinPWM=0 AND AllowStop=true`.

**The predictive blend (`smart_builders.go` `buildBlendFn`), runs after the safety clamp,
before hysteresis:** pull Layer-B snapshot (`Coupling.Shard(chID).Read()`), Layer-C snapshot
(`Marginal.Shard(chID, signatureLabel).Read()`), Layer-A snapshot (`LayerA.Read(chID)`); derive
`confA/confB/confC`; call `Aggregator.Tick(...)` → `w_pred ∈ [0,1]`; call `Blended.Compute(...)`
→ `output = w_pred·predictive + (1-w_pred)·reactive`. Predictive is IMC-PI with magnitude gain
`K = |b_ii/(1-a)|` (signed-gain caused positive-feedback instability). Three refusal gates can
drop the predictive arm: instability guards, Path-A saturation (Layer-C ΔT < 2 °C), and the
acoustic-budget gate (R33 dBA proxy capped at the preset target). Result is re-clamped and cached
in `Decisions` for the `/api/smart/channels` UI.

**The confidence aggregation order is LOCKED** (R12 §Q3, `internal/confidence/aggregator`):
(1) per-layer drift decay → (2) MIN-collapse across A/B/C → (3) low-pass filter (τ=30s) →
(4) Lipschitz clamp (≤0.05/s). Plus a cold-start hard pin (`w_pred=0` for 5 min after Envelope-C)
and a global gate that bypasses the clamp to drop straight to safe.

**Background learning loops** (independent goroutines, ctx-scoped, one per daemon lifetime):
- `internal/probe/opportunistic` — idle-gated active probing of unvisited PWM bins; writes via `polarity.WritePWM`, feeds the observation log + the signguard sign-vote detector.
- `internal/observation` — the per-tick log that re-warms Layer-B/C on restart. **The controller→estimator bridge (`cmd/ventd/smart_obs_bridge.go`) is what feeds the RLS estimators**; its absence pre-v0.5.37 stranded them at θ=[0,0] (issue #1033).
- `internal/envelope` — one-shot Envelope-C/D thermal characterisation after the strict idle gate clears.

**Exit safety:** the `watchdog` (1.8 s restore budget, under systemd's 2 s `WatchdogSec`) restores
firmware-auto on exit/panic; `ventd-recover.service` is the `OnFailure=` backstop.

> Open issue worth knowing: convergence on 8+ channel boards (NCT6687D worked example, #1253) is
> the active smart-mode problem. `driftFlags`/`wPredSystem` global gate are stubbed as of v0.5.9.

---

## 4. Package atlas — control loop and smart mode

For each package: responsibility · key types/entry points · binding rule file (under `docs/rules/`).

### Control loop and curves
| Package | Responsibility | Key types | Rule |
|---------|----------------|-----------|------|
| `internal/controller` | Per-fan loop: read sensors → curve → clamp → optional blend → write | `Controller`/`New`/`Run`/`tick`; `BlendedController`/`Compute`; `BlendFn`; `DecisionCache` | `blended-controller.md` |
| `internal/curve` | `Curve` interface + built-ins | `Curve`, `StatefulCurve`, `PIState`; `linear/points/mix/fixed/pi.go` | (blend interaction: `blended-controller.md`) |
| `internal/calibrate` | Fan calibration sweep + allow-stop gate + zero-PWM sentinel | `Manager`/`New`; `VerifyAllowStopGate`; `NewZeroPWMSentinel`; `DefaultCalibrationPath` | `calibration*.md`, `calibrate-persist.md` |
| `internal/validity` | PR-2b channel-validity probe (polarity/stall/phantom/BIOS-override) → calibration JSON | `ChannelProber`; `ProbeBIOSOverride` (note: `package validity`, stale `// Package calibration` doc) | `calibration.md` |
| `internal/polarity` | Resolve channel polarity (normal/inverted/phantom/probational); owns inversion-correct write | verdict consts; `HwmonProber`/`ProbeAll`; `Persist`/`ApplyOnStart`; `WritePWM`; `IsControllable` | `polarity.md` |
| `internal/watchdog` | Per-channel `pwm_enable` restore registry + per-syscall deadlines | `Watchdog` (Register/RestoreOne/Deregister/RestoreCtx); `DefaultRestoreBudget=1800ms` | `watchdog-safety.md` |
| `internal/cooling` | Chassis-cooling-capacity estimator vs CPU TDP (#1285); pure data, no I/O | `FanInput`; `EstimateChassisCapacityW` | — |

### Smart-mode learning stack
| Package | Responsibility | Key types | Rule |
|---------|----------------|-----------|------|
| `internal/signature` | Workload-signature learning: hash `/proc/PID/comm` (SipHash, per-install salt) into an opaque label keying B/C state | `Library`/`NewLibrary`/`Label`; `Hasher`; eviction (>30 days) | `signature.md` |
| `internal/coupling` (Layer B) | Per-channel thermal-coupling RLS (`T_i[k+1]=a·T_i+Σb_ij·pwm_j+c·load`); exposes `conf_B` | `Runtime`/`NewRuntime`/`Run`/`Shard`; `Snapshot.Read()` (lock-free); `MaxNCoupled=16` | `coupling.md` |
| `internal/coupling/signguard` | Wrong-direction-prior detector; ≥5/7 sign-vote agreement promotes a channel | `Detector`/`NewDetector` | `signguard.md` |
| `internal/marginal` (Layer C) | Per-(channel,signature) marginal-benefit RLS (`ΔT_per_+1_PWM`); saturation flag + `conf_C` | `Runtime`/`OnObservation`/`Shard`; `SaturationDeltaT=2.0` | `marginal.md` |
| `internal/confidence/layer_a` | `conf_A = tier_ceiling × √coverage × (1-residual) × recency` | `Estimator`/`Observe`/`Read`/`MarkFirstContact` | `confidence-layer-a.md` |
| `internal/fallback` | R8 fallback-tier classifier [0–7]; tier ceiling clamps predictive trust | `SelectTier(ch)` | (spec; via `confidence-layer-a.md`) |
| `internal/confidence/aggregator` | Collapse (conf_A/B/C) → `w_pred` per tick + 5-state UI label | `Aggregator`/`New`/`Tick`; `Snapshot` | `confidence-aggregator.md` |
| `internal/smartmode` | **Test-only** cross-spec integration tests (`doc.go` only — no runtime code) | — | — |
| `internal/observation` | Passive per-tick log; re-warms B/C on restart, feeds doctor | `Writer`/`New`; `Reader`; `Record`; `schemaVersion=2` | `observation.md` |
| `internal/probe/opportunistic` | Idle-gated active probing of unvisited PWM bins (one 30 s probe at a time) | `Detector`; `Scheduler`/`Run`; `CooldownWindow=7d` | `opportunistic.md` |
| `internal/idle` | Idle/quiescence gate: strict (`StartupGate` → Envelope-C) and soft (`OpportunisticGate`) | `StartupGate`, `OpportunisticGate`, `GateConfig` | `idle.md` |
| `internal/envelope` | Background Envelope-C/D thermal characterisation after idle gate | `Prober`/`NewProber`/`Probe`; `LookupThresholds` | `envelope.md` |

> The smart-mode runtime wiring lives in `cmd/ventd/smart_builders.go` + `smart_obs_bridge.go`,
> **not** in `internal/smartmode`. Rationale doc: `docs/rules-rationale/smart-mode-wiring.md`.

---

## 5. Package atlas — Hardware Abstraction Layer (HAL) and backends

### The HAL contract (`internal/hal/`)
- **`backend.go`** — `FanBackend` interface: `Enumerate(ctx)`, `Read(ch)` (must not mutate, sets `Reading.OK=false` on transient failure rather than erroring), `Write(ch, pwm uint8)` (forwards the exact 0–255 byte; controller owns clamping; owns mode transitions), `Restore(ch)` (idempotent), `Close()`, `Name()`. Channel caps bitset: `CapRead/CapWritePWM/CapWriteRPMTarget/CapRestore/CapWritePowerProfile`. Optional `PowerProfileBackend` (msi-ec shift_mode, etc.).
- **`registry.go`** — process-wide map guarded by RWMutex. `Register(name, b)` called **explicitly** from `cmd/ventd` (not `init()` — `go-conventions.md` forbids package init). `Enumerate(ctx)` fans out in sorted-name order and prefixes channel IDs `name:` (e.g. `hwmon:/sys/.../pwm1`). `Resolve(id)` splits on the first `:`.
- **Rule: `hal-contract.md`** (RULE-HAL-001..008). RULE-HAL-008: a repeat `Write` to an already-acquired channel must not re-issue the mode command (some firmware treats a repeat `pwm_enable=1` as a curve-timer reset → audible spike).

### The backends (14 dirs under `internal/hal/`)
| Backend | Hardware | Rule | Note |
|---------|----------|------|------|
| `hwmon` | Generic Linux hwmon (Super-I/O, EC, SoC) — wraps `internal/hwmon` | `hwmon-safety.md`, `hwmon-sentinel.md` | Sentinel rejects 0xFFFF RPM and >25000; temp cap 150 °C |
| `nvml` | NVIDIA GPUs (wraps `internal/nvidia` dlopen) | `gpu.md`, `nvml-helper.md` | 500 ms per-syscall deadline; writes proxied via SUID helper |
| `gpu` + `gpu/amdgpu` + `gpu/nvml` | GPU dispatcher; AMD sysfs (RDNA1/2 pwm1, RDNA3+ fan_curve); NVIDIA capability prober | `gpu.md`, `experimental.md` | AMD writes need `--enable-amd-overdrive` (kernel taint). Intel Arc read-only |
| `ipmi` (+`proto`) | Server BMC fans via `/dev/ipmi0` raw ioctls | `ipmi-safety.md` | DMI-gated to server vendors; HPE iLO refused (needs Advanced) |
| `legion` | Lenovo Legion via OOT `legion_laptop` | `RULE-HAL-LEGION.md` | State-switcher: PWM→platform_profile bucket + powermode + debugfs curve |
| `lenovoideapad` | Lenovo IdeaPad/Yoga via `ideapad_laptop` | `RULE-HAL-LENOVO-IDEAPAD.md` | Exclusive: requires `ideapad_laptop` loaded AND `legion_laptop` NOT |
| `thinkpad` | ThinkPad via `/proc/acpi/ibm/fan` | `RULE-HAL-THINKPAD.md` | Quantises 0–255 → firmware level 0–7; beats the 120 s kernel fan_watchdog |
| `msiec` | MSI laptops via OOT `msi-ec` | (firmware catalogue is generated) | Quantises PWM→named EC modes; `firmware_catalogue.go` from `tools/gen-msiec-firmware-catalogue` |
| `nbfc` | EC-locked laptops via upstream nbfc register maps; routes through `internal/ec` + `internal/acpi` | `RULE-NBFC-A/B1/B2/B3.md` | Refuses Lua configs; register-allowlist gated |
| `asahi` | Apple Silicon via `macsmc_hwmon` (detect via `/proc/device-tree/compatible`) | — | Monitor-only by Asahi policy; delegates to hwmon |
| `crosec` | Chrome OS EC via `/dev/cros_ec` ioctls | — | Duty is 0–100 %, not 0–255 |
| `pwmsys` | ARM SBCs via generic `/sys/class/pwm` | (`sysclass.md` indirect) | Default 25 kHz period on fresh channels |
| `liquid` + `liquid/corsair` | Corsair Commander Core/Core XT/ST USB AIO | `liquid-safety.md` | Pump never below `pump_minimum` (default 50), enforced in HAL; serialised writes |
| `usbbase` (+`hidraw`) | Shared pure-Go Linux hidraw substrate (no CGO) used by liquid/crosec | `hidraw-safety.md` | Enumerate filters to BUS_USB; real hidraw needs the `hidraw` build tag |

**Writes-on-by-default posture** (per project policy): laptop/EC/liquid backends write
unconditionally once `Enumerate` returns a channel — no `--enable-X` opt-in. Safety = closed-set
enums + EC/register allowlists + watchdog restore + battery/container daemon refusal. The only
genuine experimental gate is AMD OverDrive (kernel taint).

---

## 6. Package atlas — detection, database, setup, diagnostics, plumbing

### Hardware detection and database
| Package | Responsibility | Key types | Rule |
|---------|----------------|-----------|------|
| `internal/probe` | Catalog-less probe: enumerate thermal sources + controllable channels from sysfs; VM/container detect. **No write syscalls** (all I/O via injected `fs.FS`) | `ProbeResult`; `ControllableChannel`; `ThermalSource`; `WipeNamespaces` | `probe.md` |
| `internal/hwmon` | The single audited choke-point for **all** hwmon sysfs I/O (~44 files) | `ReadValue`; `AutoloadModules`; `RecoverAllPWM`; `StableDevice`; `MonitorSwap`; `InstallDriver`; `SignModuleFile`; `allowedModprobeOptions` | `hwmon-safety.md`, `hwmon-sentinel.md`, `modprobe-options-write.md` |
| `internal/hwdb` | Hardware database matcher (3 schema generations coexist) | **Live:** `profile_v1.go`/`profile_v1_1.go` (`BoardCatalogEntry`, `Catalog`), `matcher_v1.go` (`MatchV1*`, 3-tier), `fingerprint.go` (`Fingerprint`=16-char sha256). **Legacy:** `schema.go` (PR1 `Profile`), `module_match.go` | `hwdb.md`, `hwdb-schema.md` |
| `internal/hwdb/nbfc` | Vendors upstream nbfc-linux catalogue (311 JSON model configs) | `Config`; `Match` | `RULE-NBFC-A.md` |
| `internal/hwdb/lmsensors` | Parse `sensors.conf` → ventd overlay YAML (`ventd import-sensors-conf`) | `EmitOverlay` | — |
| `internal/hwdb/dtfake` | Synthetic `/proc/device-tree` MapFS for tests | — | — |
| `internal/sysclass` | Classify host (HEDT/desktop/server/laptop/miniPC/NAS) → Envelope-C params + server-probe gate | `SystemClass`; `detectWithDeps`; `AmbientBoundsOK`; `ServerProbeAllowed` | `sysclass.md` |
| `internal/ec` | Pure-Go EC register transport for nbfc (ec_sys debugfs / `/dev/port` ACPI handshake) | `Transport`; `WithAllowlist`; `LockdownActive` | `RULE-NBFC-B1.md` |
| `internal/acpi` | Userspace ACPI method bridge via `/proc/acpi/call` (OOT acpi_call) | `Bridge`; `Available` | `RULE-NBFC-B3.md` |
| `internal/platformprofile` | Observe **and drive** kernel `/sys/class/platform-profile/` (changes firmware thermal envelope, no PWM write) | `Controller`; `Selector`; `LearningStore` | — (active by default) |
| `internal/nvidia` | NVML temp/fan via purego dlopen (`libnvidia-ml.so.1`, no CGO) | `Init`; `Available`; `SetFanControlPolicy`; `HelperPath` | `nvml-helper.md` |

**The hwdb catalog** lives under `internal/hwdb/catalog/` and is embedded: **31** board files
(`boards/<vendor>.yaml`, `board_profiles:` lists of `BoardCatalogEntry`), **7** chip files,
**41** driver files. Matching is three-tier: Tier-1 exact DMI fingerprint (conf 0.9), Tier-1.5
chip-probe by hwmon name for mini-PCs whose BIOS leaves DMI as `"Default string"` (conf 0.85),
Tier-3 chip-family fallback (conf 0.6). Each board entry declares exactly one of
`dmi_fingerprint` / `dt_fingerprint` / `chip_probe`. **Navigate board data via
`profile_v1.go` + `profile_v1_1.go` + `matcher_v1.go`, not the legacy `schema.go`.**

### Setup, preflight, doctor, diagnostics, recovery
| Package | Responsibility | Key types | Rule |
|---------|----------------|-----------|------|
| `internal/setup` (+`conflicts`, `orchestrator`) | First-boot wizard: discover → calibrate → write config (CLI `--setup` + web). `conflicts` catalogues competing fan daemons and detects them so ventd takes exclusive control | `RunBlocking`/`Start`/`Progress`; `Registry`/`Detect`; PhaseGates | `setup.md`, `wizard-gates.md`, `RULE-SETUP-REPROBE-01.md` |
| `internal/preflight` (+`checks`) | Iterative install-time precondition chain (SB/MOK, kernel headers, in-tree conflict, stale DKMS, port-held); groups by severity; single queued reboot | orchestrator; `DefaultOptions` | `preflight-orchestrator.md`, `preflight-comprehensive.md`, `install-contract.md` |
| `internal/doctor` (+`detectors`) | Runtime post-install health surface (recovery cards). One detector per file (pure-read; OK facts emitted — no silent passes). ~24 detectors fit a 2 s budget | `Detector`; `Fact`; `Report`; `Suppression` | `doctor.md` |
| `internal/diag` (+`detection`, `redactor`) | Redacted diag bundle (`tar.gz`). `redactor` (16 files) strips serials/IPs/MACs/hostnames with a self-check; fuzz-tested | `bundle.go`; `Item`; `Redactor`; `MappingStore` | `diag.md` |
| `internal/hwdiag` | Canonical type + store for hardware diagnostics the web UI surfaces (`/api/hwdiag`) | `Component` (field names are a UX/wire contract) | — |
| `internal/recovery` | Cross-cutting failure classifier + remediation catalogue (#800), shared by wizard + doctor | `Classify(phase, err, journal)`; `Remediation` | `wizard-recovery.md` |
| `internal/grub` | Idempotent kernel-cmdline additions via `/etc/default/grub.d/*.cfg` | `DropInPath` | — |

### State, config, and supporting plumbing
| Package | Responsibility | Key types | Rule |
|---------|----------------|-----------|------|
| `internal/config` | Schema, load/parse, startup-retry loader, hwmon path resolution, schedule, migration | `Config`; `Fan`; `Control`; `Load`; `LoadForStartup`; `Config.Smart` | `smart-preset.md` |
| `internal/state` | Three persistent stores (KV YAML, Blob, append-only Log) under `/var/lib/ventd/`; PID lock; version/migration | `State`/`Open`; `KVDB`; `BlobDB`; `LogDB`; `DefaultDir` | `state.md` |
| `internal/iox` | Atomic-write helper (tmp+fsync+rename) | (R28 helper) | `iox.md` |
| `internal/experimental` | Experimental-flag merge/publish (CLI > config > default) | `Flags`; `Merge`; `Publish` | `experimental.md` |
| `internal/sdnotify` | systemd `Type=notify` readiness + watchdog heartbeat (no-op off systemd) | `StartHeartbeat`; `Notify` | — |
| `internal/lastfatal` | Persist a one-line fatal summary so the next start can name what failed (#1165) | `Write`; `Clear` | — |
| `internal/monitor` | Live hwmon scan for `/api/hardware` | `Scan` | — |

Other small packages not separately tabled: `internal/acoustic/*` (mic capture/proxy/stall — `acoustic-*.md`),
`internal/diag/detection`, `internal/marginal`, `internal/proc`, `internal/sysclass`, `internal/preflight/checks`,
`internal/packaging`, `internal/ndjson`, `internal/setup/orchestrator`. When in doubt, grep `docs/rules/INDEX.md`
for the subsystem name — most have a bound rule file.

---

## 7. Web UI and HTTP API

**Server (`internal/web/`, ~78 files).** Entry `web.New(...)` (`server.go`) builds one
`*http.ServeMux`, wires routes centrally, composes middleware, and launches `runHistorySampler`
+ `runScheduler`. `ListenAndServe(addr, cert, key)` binds; `Shutdown()` drains.

- **Routing** (`router.go` + `server.go`): static pages via `registerWebPage` (HTML no-cache, CSS/JS ETag+1h); every API route registered twice — `/api/<name>` **and** `/api/v1/<name>` — from one `apiRoute` table (`server.go`). Catch-all `/api/` → JSON 404.
- **Middleware** (`security.go`, `csrf.go`): global `securityHeaders` (strict CSP, no `unsafe-inline`) → `originCheck`; per-route `requireAuth` → `requireCSRF` → `requireMaxBody(1 MiB)`. Per-IP login rate limiter.
- **Health** (`health.go`): `ReadyState` (all-atomic). `/healthz` 200 once `SetHealthy`; `/readyz` 200 only if watchdog pinged AND a sensor read within 5 s. Both unauth.
- **Streaming** (`sse.go`, `setup_events_sse.go`): SSE only, **no websockets**. `/api/events` streams a `status` frame every 2 s; `/api/setup/events` streams the wizard activity feed (`?since=` resume).
- **TLS** (`selfsigned.go`, `http_redirect.go`): self-signed P-256 cert on first boot; a plaintext-on-TLS-port connection gets a 301 to `https://`.

**Auth (`internal/web/authpersist/`, `auth.go`, `csrf.go`).** Credentials live in `auth.json`
(bcrypt cost 12), **separate from config.yaml** so no config write can clobber the admin hash.
First-boot: `GET /login` 302→`/setup`; `POST /login` with `new_password` creates the admin
account (no setup token — eliminated v0.5.8.1). Sessions are in-memory (lost on restart).
Cookies: `ventd_session` (HttpOnly, SameSite=Strict) + `ventd_csrf` (readable by JS for the
synchroniser-token header injected in `web/shared/brand.js`).

**API endpoint groups** (full table with handler files: ask the source — `server.go` route table
~`389`–`495`). Auth = `requireAuth`+`requireCSRF`+body-cap unless noted:

- **Unauth probes:** `/healthz`, `/readyz`, `/api/ping`, `/api/auth/state`, `/api/version`.
- **Status/config/hardware:** `/api/status`, `/api/events` (SSE), `/api/config` (GET/PUT/PATCH), `/api/config/dryrun`, `/api/hardware`, `/api/hardware/inventory`, `/api/hardware/rescan`, `/api/history`, `/api/debug/hwmon`.
- **Profiles/schedule:** `/api/profile`, `/api/profile/active`, `/api/profile/schedule`, `/api/schedule/status`.
- **Panic:** `/api/panic`, `/api/panic/state`, `/api/panic/cancel`.
- **Calibration/probe:** `/api/calibrate/{start,status,results,abort,reset}`, `/api/probe/opportunistic/status`, `/api/detect-rpm`.
- **Setup/wizard:** `/api/setup/{status,events,start,apply,apply-monitor-only,reset,calibrate/abort,load-module}`, `/api/admin/factory-reset`, `/api/system/reboot`.
- **hwdiag one-click fixes (POST):** `/api/hwdiag` (GET report) + `/api/hwdiag/{install-kernel-headers,install-dkms,load-apparmor,mok-enroll,grub-cmdline-add,modprobe-options-write,reset-and-reinstall}`.
- **System introspection (GET):** `/api/system/{watchdog,recovery,security,diagnostics}`.
- **Diag:** `/api/diag/bundle`, `/api/diag/send`, `/api/diag/download/<name>`.
- **Smart-mode telemetry:** `/api/confidence/status`, `/api/confidence/preset` (GET/PUT), `/api/smart/status`, `/api/smart/channels`, `/api/doctor`, `/api/platform-profile`.
- **Update/release:** `/api/update/check`, `/api/update/apply`, `/api/release-notes`, `/api/set-password`.

**Frontend (`web/`).** 13 vanilla pages (no framework/build): `dashboard`, `calibration`,
`curve-editor`, `devices`, `doctor`, `hardware`, `health`, `schedule`, `sensors`, `settings`,
`setup`, `smart`, `login`. Shared design system in `web/shared/` (`tokens.css`, `shell.css`,
`brand.css`, `ambient.css`, `brand.js`, `sidebar.html`). The **calibration page** is the
animation showcase (live RPM gauges, scatter). **"No theatre" constraint** (`web-ui.md`,
`ui.md`, `usability.md`): every visible affordance must consume a real backend signal — no
client-side fake forecasts or animations.

> **Doc drift:** `docs/api.md` still documents a setup-token login flow and a `session` cookie.
> The code uses first-boot `new_password` + `ventd_session`. **Code is canonical.**

**Binding rules:** `web-ui.md` (CSRF/cookie/body-cap/update-staging), `ui.md` (spec-12: no
external CDN, token-only colours, byte-matched sidebar), `usability.md` (zero-terminal-after-install).

---

## 8. Engineering invariants — the `docs/rules/` system

This is where the load-bearing invariants live (per project convention, **not** in `CLAUDE.md`).

- **`docs/rules/INDEX.md`** — the canonical map of all 62 rule files. Read it first; open a specific rule only for full text. Regenerate with `go run ./tools/rule-index` (`--check` fails CI if stale).
- Each rule file states invariants as `## RULE-XXX` headings, many with a **`Bound:` line** naming a leaf subtest. `tools/rulelint` asserts that bound **leaf** subtest name exists via `t.Run(...)` — **`Parent/Leaf` form silently fails**, use the leaf name only.
- **`docs/rules-rationale/`** — long-form "why" behind rule families (smart-mode wiring, opportunistic soft-idle, hwmon runtime monitors).
- Rule families map 1:1 to the subsystems in §4–§7. The fast path from a symptom to the governing invariant is: grep the package name in `INDEX.md` → open the rule → find the bound test.

---

## 9. Build, CI, release, tooling

**Build.** `Makefile` targets shell out to scripts/tools (no new deps): `build` (goreleaser
snapshot), `test` (`go test -race ./...`), `lint`, `cover`, `pre-push` (runs `scripts/ci-local.sh`
— the full local CI mirror), `verify-repro` (two builds → identical sha256), `sync-embeds`
(mirror `install.sh` + `CHANGELOG.md` into `internal/web`), `sbom`, `sync-nbfc-configs`.
`.goreleaser.yml` builds 5 binaries (`ventd`, `ventd-recover`, `ventd-ipmi`, `ventd-nvml-helper`,
`ventd-musl` with build tag `nonvidia`), all `CGO_ENABLED=0` linux amd64+arm64, `-trimpath`,
reproducible `mod_timestamp`; emits SBOMs, cosign signatures, deb+rpm via nfpm.

**CI (`.github/workflows/`, 20 files).** `ci.yml` is **the canonical gate** (build matrix across
distros, lint golangci v2.1.6, cross-compile, shellcheck, nix-drift, apparmor-parse). Others:
`meta-lint.yml` (the repo-tooling gate: rulelint + rule-index --check + regresslint + actionpincheck),
`e2e.yml` (headless-chromium UI), `hil-smoke.yml` (self-hosted hardware, pre-tag), `fuzz-{smoke,long}.yml`,
`drift-{detect,baseline-update}.yml`, `allowlist-check.yml`, `no-ai-attribution.yml`, `release.yml`,
`verify-reproducible.yml`, `govulncheck.yml`, `docker.yml`, `lychee.yml`, `drew-audit.yml`,
`pre-release-check.yml`, `pve-template-prep.yml`, `runner-smoke.yml`, `auto-label.yml`, `branch-cleanup.yml`.
Action pinning is enforced (`ci-action-pinning.md`: 40-char SHA or v-tag, version comment, allowlist).

**Repo tooling (`tools/`):** `rulelint` (Bound-line → leaf-subtest enforcement), `rule-index`
(regenerates `INDEX.md`), `allowlistcheck` (PR diff vs the `Allowlist:` glob in the PR body),
`actionpincheck` (action-pinning rules), `regresslint` (closed bug → `TestRegression_Issue<N>_*`),
`driftreport` (coverage/size/dup/dead-code drift), `audit/ghost-code` (exported-but-uncalled),
`golangcidrift` (triple-pin the golangci version), `gen-msiec-firmware-catalogue` (regenerates the
msi-ec firmware table), `orchestrator-hil-probe`.

**Scripts (`scripts/`):** `install.sh` (canonical curl-pipe-bash installer) + `uninstall.sh` +
nfpm hooks (`pre/postinstall`, `pre/postremove`); `ci-local.sh` + `verify-local.sh`;
**`install-git-hooks.sh`** — install this first on a fresh clone; its pre-push runs `ci-local.sh`,
mirroring the remote gate. Init-system files: `ventd.openrc`, `ventd.runit`. Changelog rebase
union-merge: `resolve-changelog-conflict.sh`. Release: `release.sh`.

---

## 10. Deploy, packaging, and test infrastructure

**Deploy (`deploy/`).** systemd units carry the safety wiring:
- `ventd.service` — `Type=notify`, `Restart=on-failure`, **`WatchdogSec=2s`**, **`OnFailure=ventd-recover.service`**, `ExecStartPre=-/usr/local/sbin/ventd-wait-hwmon`. `User=root` with a heavy sandbox (`ProtectSystem=strict`, enumerated `ReadWritePaths`, `SystemCallFilter`, `MemoryDenyWriteExecute`). AppArmor profile shipped but **not** auto-attached.
- `ventd-recover.service` — `Type=oneshot`, resets every `pwm_enable=1` on failure.
- `ventd-ipmi.service` — sidecar, `User=ventd-ipmi`, `CAP_SYS_RAWIO`, `BindsTo` ventd.service, `ConditionPathExists=/dev/ipmi0`.
- `ventd-postreboot-verify.service` — opt-in post-reboot fan verification.
- udev: `90-ventd-hwmon.rules` (chgrp pwm files to `ventd`), `90-ventd-liquid.rules` (Corsair hidraw uaccess), `50-ventd-update.rules` (polkit for in-UI update). sysusers/tmpfiles drop-ins. SELinux `.te`/`.fc`.

**Packaging (`packaging/`):** `docker/` (alpine musl `nonvidia`), `aur/` (`ventd` + `ventd-bin`),
`nix/` (`flake.nix`, `nixos-module.nix`, drift check).

**Test infrastructure (388 `*_test.go`).** Hardware fakes under `internal/testfixture/`:
`fakehwmon`, `fakehid` (+corsair), `fakeipmi`, `fakedmi`, `fakecrosec`, `fakedt`, `fakeprocsys`,
`fakepwmsys`, `faketime`, `base`. Call-recording helpers in `testutil/`. `validation/` holds
tiered HIL/VM records (`tier-0.3/0.5/2/3`, `fresh-vm-smoke/`, `install-smoke/`). `soak-results/`
holds field soak logs. `COVERAGE.md` is the last coverage snapshot.

---

## 11. Config schema (`config.example.yaml`)

Top-level sections: `poll_interval`; `web` (listen, password_hash, optional tls_cert/tls_key,
session_ttl, trust_proxy); `sensors[]` (name/type[hwmon|nvidia]/path); `fans[]`
(name/type/pwm_path/min_pwm/max_pwm, optional is_pump+pump_minimum); `curves[]`
(linear / mix[function:max, sources] / pi[setpoint,kp,ki,feed_forward,integral_clamp]);
`controls[]` (fan↔curve bindings). Smart-mode + idle + envelope + experimental sub-sections are
documented in `docs/config.md`. The header documents the reference rig (MSI Z790 / i9-13900K /
RTX 4090, NCT6687D).

---

## 12. "Where do I find X?" cheat sheet

| I want to… | Go to |
|------------|-------|
| Understand daemon startup / shutdown | `cmd/ventd/main.go` `run` → `runDaemonInternal`; this doc §3 |
| Find where a PWM byte actually gets written | `internal/polarity` `WritePWM` ← `internal/controller` `tick`; HAL backend `Write` |
| Add/understand a hardware backend | `internal/hal/backend.go` (contract) + the backend dir; `hal-contract.md` |
| Add a board to the catalog | `internal/hwdb/catalog/boards/<vendor>.yaml` (`BoardCatalogEntry`); matcher in `matcher_v1.go`; `hwdb-schema.md` |
| Trace the smart-mode learning math | §3 + `internal/{coupling,marginal,confidence/*,signature}`; wiring in `cmd/ventd/smart_builders.go` |
| Find an HTTP endpoint's handler | `internal/web/server.go` route table (`apiRoute`), then the named `handle*` func; §7 |
| Understand an invariant / why code is shaped a way | `docs/rules/INDEX.md` → the rule file → the bound test |
| See safety/restore wiring | `internal/watchdog`, `cmd/ventd-recover`, `deploy/ventd.service` (`OnFailure`, `WatchdogSec`) |
| Reproduce CI locally | `bash scripts/install-git-hooks.sh` then `make pre-push` (runs `scripts/ci-local.sh`) |
| Add a diagnostics fix card | `internal/doctor/detectors/` (one file per detector) + the `/api/hwdiag/*` handler |
| Persist something across restarts | `internal/state` (KV/Blob/Log under `/var/lib/ventd/`); `state.md` |

---

## 13. Known gotchas and stale docs (verify against code)

- `docs/api.md` documents the old setup-token login + `session` cookie; code uses `new_password` + `ventd_session`. **Code wins.**
- `internal/validity` is `package validity` but its doc comment still says `// Package calibration`.
- `internal/hwdb/schema.go` defines the **legacy** PR1 `Profile`; the live board data uses `BoardCatalogEntry` (`profile_v1_1.go`) + `matcher_v1.go`.
- `cmd/ventd/main.go` line ~633 marks `obsWriter` "consumed in a follow-up spec" but it is already wired via `buildSmartObsBridge` — stale comment.
- After `rmmod`+`modprobe` of a module ventd uses, the `hwmonN` index may change; `internal/hwmon/swap_monitor.go` re-resolves on a 10-min interval, but a restart is the reliable recovery.
- `driftFlags` and the `wPredSystem` global gate in `buildBlendFn` are stubbed as of v0.5.9 (future work).
