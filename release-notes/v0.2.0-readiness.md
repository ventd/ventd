# v0.2.0 tag readiness

Last updated: 2026-04-16T04:00Z by overnight ops session.

Phoenix reads this first thing in the morning. If the blocker list is
empty, the rig re-verify is green, and the VM matrix is green,
`git tag v0.2.0` is cleared. Tagging is explicitly Phoenix's call;
this session does not tag.

## Blockers (must be closed before tag)

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#58](https://github.com/ventd/ventd/issues/58) — OnFailure=ventd-recover.service placed under [Service], silently dropped | **OPEN** | — | Watchdog recovery promised by PR #25 does not fire; one-line move of the directive from `[Service]` to `[Unit]` in `deploy/ventd.service`. Ship-stopper. |

## Non-blockers surfaced by rig re-verify, decide-scope before tag

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#59](https://github.com/ventd/ventd/issues/59) — config migration for `web.tls_cert`/`tls_key` | **OPEN** | — | Pre-#5 configs crashloop on upgrade because `Load` doesn't auto-populate TLS paths when `/etc/ventd/tls.{crt,key}` already exist. Rig workaround: hand-append two lines. Phoenix's call whether to block v0.2.0. |
| [#60](https://github.com/ventd/ventd/issues/60) — install.sh doesn't refresh unit files on upgrade | **OPEN** | — | Running `install.sh` on an already-installed box copies the binary but not the unit, so upgraders stay on stale systemd. Related to #58: shipping the #58 fix is useless if operators never get the new unit. Phoenix's call. |

## Closed pre-tag work

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#36](https://github.com/ventd/ventd/issues/36) — setup wizard writes config referencing kernel modules not loaded at scan time | CLOSED 2026-04-15 | PR #56 | Setup now rejects configs the resolver would refuse on the next boot (`70415b8`). |

## Validation state

| Gate | Result | Source |
|------|--------|--------|
| v0.2.0 final rig re-verify on phoenix-MS-7D25 | **FAIL** — 1 P0 (#58), 3 P1/informational | PR [#54](https://github.com/ventd/ventd/pull/54) — see `validation/phoenix-MS-7D25-v0.2.0-final.md` |
| Fresh-VM install smoke — Ubuntu 24.04, Debian 12, Fedora 42, Arch | **PASS 9/9 × 4** | PR [#50](https://github.com/ventd/ventd/pull/50), reports under `validation/fresh-vm-smoke-*.md` |
| Fresh-VM install smoke — Proxmox-driven variant | MERGED | PR [#51](https://github.com/ventd/ventd/pull/51) |
| Alpine (musl) | **expected FAIL** — glibc binary, separate wave | Documented in `validation/install-smoke/install-smoke-alpine.md` |

## Release ops state

| Item | State | Source / next step |
|------|-------|--------------------|
| `release-notes/v0.2.0.md` | MERGED | PR [#52](https://github.com/ventd/ventd/pull/52). Audit vs `git log v0.1.1..HEAD` is tracked under this session; any user-visible PR that lands overnight amends it. |
| `CHANGELOG.md` v0.2.0 entry | MERGED | PR [#46](https://github.com/ventd/ventd/pull/46). Will be finalised when #58 lands. |
| README landing-page polish + first-boot screenshot | MERGED in main via #52; PR [#53](https://github.com/ventd/ventd/pull/53) carries the screenshot + TODO(dashboard.png) | Rebased onto main tonight; only the screenshot commit survived (polish already upstream). Awaiting CI then squash-merge. |
| `docs/images/dashboard.png` (issue [#62](https://github.com/ventd/ventd/issues/62)) | OPEN | Needs a screenshot from a hardware-populated daemon. No fabricated image will land. |
| `goreleaser check` + `goreleaser --snapshot --clean` | not yet run this session | Runs after #58 lands (the fix changes the shipped unit; goreleaser needs to pick it up). See the goreleaser section below once it runs. |
| Repo homepage URL (`https://github.com/ventd/ventd`) and topic list | not yet set | Pending PAT scope check; otherwise a web-UI todo filed for Phoenix. |

## Checklist for tagging

Tag is Phoenix's call. Suggested gate:

- [ ] #58 merged and re-verified on phoenix-MS-7D25 with post-SIGKILL recovery test.
- [ ] Decision on #59 and #60 (in-scope for v0.2.0 or deferred to v0.2.1).
- [ ] VM matrix still PASS on the tip of `main` at tagging time (re-run
      `validation/fresh-vm-smoke.sh ubuntu-24.04 debian-12 fedora-42 arch`).
- [ ] `goreleaser check` clean on the tagging commit.
- [ ] `release-notes/v0.2.0.md` covers every user-visible PR in `git log v0.1.1..HEAD`.
- [ ] `docs/images/dashboard.png` landed (or explicit decision to ship without).

When those are all green: `git tag -a v0.2.0` on `main` tip, `git push github v0.2.0`,
GoReleaser runs automatically via `.github/workflows/release.yml`.

## Overnight ops log

Rolling log of what this session touched. Appended to as items land.

- 2026-04-16T04:00Z — session started. PR #53 rebased onto latest main; force-pushed, awaiting CI.
- 2026-04-16T04:00Z — readiness doc created (this file).
