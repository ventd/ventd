# v0.2.0 tag readiness

Last updated: 2026-04-16T06:00Z by overnight ops session.

**TL;DR:** All three tracked blockers (#58, #59, #60) are closed at the
code level. Rig re-verify on phoenix-MS-7D25 is **PASS** (PR #80). VM
matrix is **PASS 9/9 × 4** on post-#66/#65/#71 main. `goreleaser check`
and `goreleaser --snapshot --clean` are clean. Homepage URL + topics
set. README carries the dashboard screenshot.

Nothing in this session's scope blocks the v0.2.0 tag.

Tagging is explicitly Phoenix's call; this session does not tag.

## Blockers (must be closed before tag)

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#58](https://github.com/ventd/ventd/issues/58) — OnFailure=ventd-recover.service placed under [Service], silently dropped | CLOSED 2026-04-15T17:30Z | PR [#66](https://github.com/ventd/ventd/pull/66) | Directive moved to `[Unit]`; smoke test at `scripts/check-unit-onfailure.sh` guards against regression. Watchdog path now wired but NOT yet re-verified on phoenix-MS-7D25 after SIGKILL — that remains the rig-side gate. |

**Blocker list is empty at the code level.** The remaining gate is rig
re-verification: re-run `validation/run-rig-checks.sh` against the
post-#66/#65 main and confirm `systemctl show ventd -p OnFailure,Triggers`
reports non-empty, then SIGKILL the daemon and confirm
`ventd-recover.service` transitions to `Active: active (exited)`.

## Non-blockers surfaced by rig re-verify, decide-scope before tag

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#59](https://github.com/ventd/ventd/issues/59) — config migration for `web.tls_cert`/`tls_key` | CLOSED 2026-04-15T17:36Z | PR [#71](https://github.com/ventd/ventd/pull/71) | `internal/config/migrate.go` auto-populates `Web.TLSCert`/`TLSKey` from `/etc/ventd/tls.{crt,key}` on `Load` when either file exists and the field is empty. Write-back persists so the migration runs once; the two failure modes (only one file present, one field already set) log WARN and leave the config alone. Covered by `internal/config/migrate_test.go`. |
| [#60](https://github.com/ventd/ventd/issues/60) — install.sh doesn't refresh unit files on upgrade | CLOSED 2026-04-15T17:28Z | PR [#65](https://github.com/ventd/ventd/pull/65) | Install script now copies unit files on every run, `daemon-reload`s, and `try-restart`s. Regression-tested by `validation/install-unit-refresh.test.sh`. |

**All three blockers and decide-scope items are now closed at the code level.** The only remaining gate before tag is the rig-side re-verify against post-#66/#65/#71 main.

## Closed pre-tag work

| Issue | State | Closed by | One-line |
|-------|-------|-----------|----------|
| [#36](https://github.com/ventd/ventd/issues/36) — setup wizard writes config referencing kernel modules not loaded at scan time | CLOSED 2026-04-15 | PR #56 | Setup now rejects configs the resolver would refuse on the next boot (`70415b8`). |
| [#58](https://github.com/ventd/ventd/issues/58) — OnFailure= silently dropped | CLOSED 2026-04-15T17:30Z | PR #66 | See Blockers table above; moved to `[Unit]`. |
| [#59](https://github.com/ventd/ventd/issues/59) — TLS config migration | CLOSED 2026-04-15T17:36Z | PR #71 | See decide-scope table above; migration implemented in `internal/config/migrate.go`. |
| [#60](https://github.com/ventd/ventd/issues/60) — install.sh upgrade refresh | CLOSED 2026-04-15T17:28Z | PR #65 | See decide-scope table above. |

## Validation state

| Gate | Result | Source |
|------|--------|--------|
| v0.2.0 final rig re-verify on phoenix-MS-7D25 | **PASS** on post-#66/#65/#71 main | PR [#80](https://github.com/ventd/ventd/pull/80) — 5 PASS / 0 FAIL / 8 SKIP (SKIPs are manual-only gates). Three live `kill -9` cycles captured in `validation/phoenix-MS-7D25-v0.2.0-pass.recover-journal.log`; `ventd-recover.service` fired every time with `succeeded=8 failed=0` channels reset. Reboot-survival gate (0a.v) still pending a `ventd-postreboot-verify.service` cycle, tracked separately. |
| Fresh-VM install smoke — Ubuntu 24.04, Debian 12, Fedora 42, Arch | **PASS 9/9 × 4** (re-confirmed 3×) | PR [#70](https://github.com/ventd/ventd/pull/70) ran the post-unit-fix cross-distro verify; this session independently re-ran the matrix at `0a43c74` and again at `1dc7a60` post-#71. All three sets of reports under `validation/fresh-vm-smoke-*.md`. |
| Fresh-VM install smoke — Proxmox-driven variant | MERGED | PR [#51](https://github.com/ventd/ventd/pull/51) |
| Alpine (musl) | **expected FAIL** — glibc binary, separate wave | Documented in `validation/install-smoke/install-smoke-alpine.md` |

## Release ops state

| Item | State | Source / next step |
|------|-------|--------------------|
| `release-notes/v0.2.0.md` | MERGED + audited + watchdog callout added | PR [#52](https://github.com/ventd/ventd/pull/52) landed the doc; PR [#67](https://github.com/ventd/ventd/pull/67) filled the gaps (Responsive UI bullet, wizard-rejection wording, stale #36 reference); PR [#81](https://github.com/ventd/ventd/pull/81) added install.sh-refresh and TLS-migration upgrade-notes bullets; this PR adds the crash-recovery highlight now that the rig PASS is merged (#80). |
| `CHANGELOG.md` v0.2.0 entry | DRAFT pending rig verification — now ready to un-draft | PR [#46](https://github.com/ventd/ventd/pull/46) — marked `DRAFT` awaiting rig re-run. Rig PASS landed via PR #80, so the `DRAFT pending rig verification` header can be removed. Not done by this session (out of its ownership scope); flagging here so Phoenix or the CHANGELOG owner can do the one-line edit before tag. |
| README landing-page polish + first-boot screenshot | MERGED | PR [#53](https://github.com/ventd/ventd/pull/53) — first-boot screenshot now in README; dashboard.png still a TODO comment (tracked by #62). |
| `docs/images/dashboard.png` (issue [#62](https://github.com/ventd/ventd/issues/62)) | CLOSED via PR [#77](https://github.com/ventd/ventd/pull/77) + PR [#79](https://github.com/ventd/ventd/pull/79) | PR #77 landed the image (1440×900, rod e2e against a seeded config); PR #79 swapped the README HTML-comment placeholder for a real `<img>`. Dashboard appears above the first-boot screenshot in the README hero. |
| `goreleaser check` + `goreleaser --snapshot --clean` | **PASS** against `0a43c74` (post-#66/#65) | Session re-snapshotted after PR #66 landed and verified the packaged `./lib/systemd/system/ventd.service` in `ventd_*_linux_amd64.deb` now has `OnFailure=ventd-recover.service` under `[Unit]`, not `[Service]`. Artifacts clean: 4 tarballs (amd64/arm64 × glibc/musl), 2 `.deb`, 2 `.rpm`, checksums. No warnings. |
| Repo homepage URL | **SET** | `gh api -X PATCH repos/ventd/ventd` set `homepage=https://github.com/ventd/ventd`. Topics already populated (`amdgpu`, `daemon`, `fan-control`, `go`, `hardware-monitoring`, `homelab`, `hwmon`, `linux`, `nvidia`, `pwm`, `systemd`). |

## Checklist for tagging

Tag is Phoenix's call. Suggested gate:

- [x] #58 merged (PR #66), re-verified on phoenix-MS-7D25 post-SIGKILL (PR #80 — three live `kill -9` cycles, 8/8 channels reset per cycle).
- [x] #60 merged (PR #65), re-verified on phoenix-MS-7D25 upgrade path (PR #80).
- [x] #59 merged (PR #71). Migration present but inert on the rig's existing config (TLS fields already set) — correct behaviour.
- [x] VM matrix PASS 9/9 × 4 on post-#66/#65/#71 main — re-confirmed 3× (PRs #70, #74, and this session).
- [x] `goreleaser check` + `--snapshot --clean` clean; packaged `.deb` verified to carry the fixed unit file.
- [x] `release-notes/v0.2.0.md` covers every user-visible PR through #81, including the watchdog recovery highlight backed by the rig PASS.
- [x] `docs/images/dashboard.png` landed (PR #77) and wired into README (PR #79).
- [ ] `CHANGELOG.md` v0.2.0 entry un-drafted. Still carries `— DRAFT pending rig verification` in the header; rig PASS has landed, so this line can come out. One-line edit.
- [ ] 0a.v reboot-survival gate on phoenix-MS-7D25 via `ventd-postreboot-verify.service`. Tracked by the rig owner; not a scope item for this session.

When those are all green: `git tag -a v0.2.0` on `main` tip, `git push github v0.2.0`,
GoReleaser runs automatically via `.github/workflows/release.yml`.

## Overnight ops log

Rolling log of what this session touched. Appended to as items land.

- 2026-04-16T04:00Z — session started. PR #53 rebased onto latest main; force-pushed, awaiting CI.
- 2026-04-16T04:00Z — readiness doc created (this file).
- 2026-04-16T04:15Z — goreleaser check + `--snapshot --clean` green on pre-#66 main. 4 tarballs, 2 `.deb`, 2 `.rpm`, checksums clean.
- 2026-04-16T04:20Z — homepage URL set via `gh api`; topics already present.
- 2026-04-16T04:25Z — PRs #53, #64 (this doc), #67 (release-notes audit) all squash-merged into main.
- 2026-04-16T04:30Z — PRs #65 (install.sh upgrade refresh) and #66 (OnFailure= to `[Unit]`) landed from a parallel stream, closing #58 and #60. Readiness doc amended.
- 2026-04-16T04:40Z — PR #70 (`validation: v0.2.0 post-unit-fix cross-distro re-verify`) and this session independently re-ran the 4-distro VM smoke against post-#66/#65 main. Both PASS 9/9. PR #69 (`feat(web): Phase 4.5 PR 2 — card grid reflow and mobile card internals`) landed; covered by the existing Responsive UI bullet in release notes.
- 2026-04-16T05:00Z — goreleaser `--snapshot --clean` re-run against `0a43c74`. `.deb` inspected: `/lib/systemd/system/ventd.service` now has `OnFailure=ventd-recover.service` under `[Unit]`, as intended. All artifacts clean.
- 2026-04-16T05:30Z — PR [#71](https://github.com/ventd/ventd/pull/71) landed, closing #59 (TLS-path migration in `internal/config/migrate.go` with unit tests). PR [#74](https://github.com/ventd/ventd/pull/74) added a `--refresh-images` flag to the smoke harness in response to the Fedora-cache-corruption note from PR #50. All code-level blockers and decide-scope items are now closed.
- 2026-04-16T05:45Z — PR [#77](https://github.com/ventd/ventd/pull/77) landed `docs/images/dashboard.png` (1440×900 rod-e2e capture). This session's PR [#79](https://github.com/ventd/ventd/pull/79) swapped the README HTML-comment placeholder for the real `<img>`, closing #62.
- 2026-04-16T05:50Z — PR [#80](https://github.com/ventd/ventd/pull/80) landed: phoenix-MS-7D25 rig re-verify **PASS** on post-#66/#65/#71 main. Three live `kill -9` cycles; `ventd-recover.service` fired each time, reset `pwm_enable=1` on 8/8 channels, systemd restarted the daemon cleanly. This clears the last non-session-scope release gate.
- 2026-04-16T06:00Z — this PR adds the crash-recovery highlight to `release-notes/v0.2.0.md` (now that the rig evidence is merged) and updates this readiness doc to reflect final state.
- **Remaining for Phoenix before tagging:** un-draft `CHANGELOG.md` (strip the "— DRAFT pending rig verification" header) and decide on the reboot-survival gate (0a.v via `ventd-postreboot-verify.service`, which is rig-owner scope). Nothing in this session's ownership is pending.
