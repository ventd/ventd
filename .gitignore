# Build output
/ventd
/bin/
/dist/

# Go test artifacts
*.test
*.out
cover.html

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store

# Local development notes (not public)
/STRATEGY.md
/HARDWARE-TODO.md
/TODO.md
/REVIEW.md
/REVIEW-FINDINGS.md
/exampleui.png
/issue1.png

# Personal masterplans — kept local on Phoenix's machine, never tracked.
/ventdmasterplan.md
/ventdtestmasterplan.md

# specs/ — Phoenix's spec corpus is being audited one spec at a time in
# separate sessions to confirm each was properly landed. Kept local only
# during that audit; do not re-track wholesale.
/specs/

# AI-assistant configuration is per-machine. Engineering invariants
# previously under .claude/rules/ now live at docs/rules/ as plain
# engineering docs; tools/rulelint enforces them in CI.
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/COPILOT.md
.claude/

# Internal dev tooling
/scripts/provision-test-vms.sh
securitytodo.md

# Real cross-distro smoke config — contains Proxmox API token
/scripts/cross-distro-smoke.config.sh

# fresh-VM smoke harness artifacts
/validation/.build/
/validation/fresh-vm-smoke-*.md
release-notes/*-dryrun.txt


# AI tooling at repo root — local-only
/.mcp.json

*:Zone.Identifier
*:Zone.Identifier:Zone.Identifier

# Git worktrees (local dev isolation)
/.worktrees/
.claude/RULE-INDEX.md

# Local screenshots dir — populated by render-screenshots tooling,
# uploaded to README by Phoenix manually. Not tracked.
/screenshots/
/ghost-code
